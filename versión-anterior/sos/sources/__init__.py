"""Adapters de fuentes externas → sobres de evidencia para WF-Ingest.

Cada adapter devuelve dicts con el sobre plano de la ingesta (ver `sos/workflows/ingest.py`).
Añadir una fuente nueva = un módulo aquí + (opcional) un feeder cron en `sos/workflows`.
"""
