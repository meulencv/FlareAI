---
tags: [happyrobot, bitácora]
date: 2026-09-19
---

# 2026-09-19 — S.O.S. Agentic Crisis Engine (noche del 18 al 19)

## Petición inicial
> "interioriza bien la idea de todo para poder hacer un backend perfecto tema base de datos también
> revisa toda la documentación […] haz un gran plan […] pero en la plataforma de ellos y muy muy
> escalable […] primero crea el plan y me lo das para revisarlo"

Más tarde: "todo en happy robot; lo que determina si algo es real […] será que una IA evalúa los
casos"; "web call simulado"; "web propia en el repo"; "como no la tengo provisionada hazla en local
[…] pero que sea muy fácil cuando tenga twin enviarla"; "todo lo que instales en un venv, no fuera
de esta carpeta"; "acaba el proyecto, que funcione bien el simulador".

## Pasos realizados
1. Revisión completa: OpenAPI (179 endpoints), doc local (`~/Downloads/happyrobot-docs-web`, 452
   ficheros), consultas reales a la org (catálogo de 278 integraciones, schemas de nodos, estado de
   Twin, teléfonos, credenciales). → [[Twin - base de datos]], [[Catálogo de nodos]], [[Apps y MCP]]
2. Plan aprobado (plan mode) con revisión del pitch: qué es viable y qué no (P2P offline no;
   teléfono real requiere número; Twin no provisionado). → [[SOS Crisis Engine - arquitectura]]
3. Cliente API, esquema Twin declarativo, config y escenario demo, motor de riesgo con tests.
4. **Laboratorio de formatos**: dos workflows temporales para descubrir el JSON exacto de cada nodo
   (paragraph/variables, key-value, Webhook body.raw, Twin, Paths, Loop, Call Workflow, Reasoning
   Agent + tools, prompt vars, web call con `data`, signals). Borrados al final. → [[Formato de nodos por API]]
5. DSL (`_builder.py`) + deployer (`deploy.py`): 9 workflows creados y publicados por API en una
   pasada. Ingesta probada con run real (nodo a nodo) y Reasoning Agent probado con tools reales.
   → [[Workflows SOS]], [[Cómo desplegar - sos deploy]], [[Reasoning Agent y tools]]
6. Twin sigue en 404 y la key sin `twin.manage` → **Postgres embebido** dentro de `.local/pg`
   (tras deshacer un `brew install` que rompía la regla "nada fuera de la carpeta"), capa `sos.db`
   con la misma interfaz que Twin, `sync-to-twin`. → [[Base de datos local embebida]]
7. **Intérprete local** de los mismos specs + políticas deterministas de los agentes →
   simulador completo `main_simulation.py` con giro de viento funcionando. → [[Intérprete local de workflows]], [[Escenario demo - giro de viento]]
8. Dashboard `web/` (mapa con cono de trayectoria, acciones HITL, sala de llamadas con web call,
   simulador integrado, config en caliente). → [[Dashboard y simulador]]
9. Feeders meteo (Open-Meteo) y FIRMS desplegados pero **despublicados** hasta tener Twin.
10. 12 tests verdes; commit `be1bb86`; documentación en este vault.

## Resultado al cierre
- Local: `python main_simulation.py` reproduce el escenario entero; dashboard en :8000.
- HappyRobot: 9 workflows SOS publicados (7 activos, 2 cron en espera). Runs de prueba visibles.
- Sin Twin no se puede probar la voz SOS ni el Reasoning Agent con datos reales (queda listo).

## Tarde: vaciado de la plataforma
Petición: "no nos va porque no tenemos BD por ahora […] elimina todo lo que hiciste en la plataforma
así después volvemos a empezar". Se borraron los 9 workflows SOS (`deploy-state.json` quedó vacío).
Descubierto: `DELETE /workflows/{id}` devuelve 400 *"Cannot delete a workflow that has a live
version"* → hay que `POST /workflows/{id}/unpublish` (production) antes; `sos destroy` ya lo hace.
Se conservan `FlareAI Web Voice` (voz de reserva del dashboard local) y `test`. No hay signal keys
ni Twin (sigue 404). Nada cambia en local: el simulador y el dashboard siguen funcionando igual.

## Pendiente / próximos pasos
- **Usuario**: activar Twin (Settings → Twin Database) y crear una API key con *Full Twin access*;
  luego `sos twin migrate && sos twin sync-to-twin && sos deploy` (recrea los 9 workflows desde cero,
  con ids nuevos) `&& main_simulation.py --cloud`.
- Probar en cloud los formatos aún no ejecutados con Twin real: salida del nodo *Query Twin with
  SQL* (asumimos `rows`), *Write to Twin* con jsonb/uuid, Loop con variables, respuesta CSV del
  nodo GET (FIRMS). Ajustar `outputs` de los specs si difieren.
- Conseguir FIRMS MAP_KEY y ponerla como variable del workflow `firms_feeder`.
- Pulir prompts del Analista y de voz con llamadas reales; medir tokens/tiempos.
- Rotar las API keys tras el hackathon (pasaron por el chat y quedan en `__headers__` de los runs).
- Opcional: feed en vivo con `/realtime/tokens`, Slack para `notify`, App de HappyRobot para el dashboard.

## Prueba SMS independiente de FlareAI

Petición: crear un workflow HappyRobot y una interfaz con botón para probar SMS. El usuario
confirmó `+15304471317` como remitente y autorizó la clave de cuenta; después aportó el valor
literal `x-api-key` del editor del workflow.

1. Se revisaron el vault y la API pública y se confirmó el número Telnyx `3052288166032573735`.
2. Se creó y publicó `FlareAI SMS Test`, workflow `01a0b8d2-28d1-7e38-b7cf-7dded8edb333`,
   versión `01a0b8d2-28e0-7dde-b3ca-b66d04d86791`. Flujo: solicitud predefinida `to`/`message`
   → Send SMS. La API valida la configuración del nodo, pero publicar devuelve avisos de variables.
3. Se creó `sms_demo.py` con interfaz `static/sms.{html,css,js}`: solo loopback, confirmación
   explícita, validación de entrada, CSRF/Host/Origin, límite de frecuencia y deduplicación en memoria.
   No se modificaron `app.py` ni la implementación archivada.
4. Pasaron 13 tests con mocks, compilación Python, comprobación de sintaxis JS y ESLint.
   La página respondió HTTP 200 en `http://127.0.0.1:8091`, con envío deshabilitado.
5. La ruta API de ejecución devolvió Cloudflare 502. Se probaron llamadas al webhook sin
   destinatario: Bearer devolvió Unauthorized; `x-api-key` devolvió Invalid API key.
6. Se ensayó también SHA-256 de la clave en la configuración del trigger; esto no resolvió
   la autenticación. No constituye un formato de autenticación verificado ni debe recomendarse.
   El usuario mostró después el valor del editor: se usó literalmente y se comprobó en memoria
   que coincide con `configuration.api_key`. Aun así producción devuelve 401, también con
   encabezado en minúsculas. No se debe asumir un error de copia o mayúsculas.

**Resultado:** workflow publicado e interfaz local creada, pero autenticación y entrega aún no
verificadas. No se enviaron SMS reales. Las credenciales se pasaron en variables de proceso,
no en archivos; una respuesta de diagnóstico incluyó la clave de cuenta y ambos secretos también
pasaron por el chat. Rotarlos al terminar, sin copiar sus valores en este vault.

**Pendiente:** guardar/republicar desde el editor manteniendo Enhanced Security; investigar la
posible diferencia entre configuración del editor y producción (hipótesis). Una vez autenticado,
comprobar variables y hacer un envío real solo con destinatario/texto autorizados. Reiniciar el
servidor con ambas variables de entorno y verificar la recepción en el móvil; completed no prueba entrega.

### Resolución posterior de la autenticación SMS

El usuario publicó Version 2, pero persistió el 401 con la clave anterior. Después regeneró
la clave desde Advanced configuration (botón de flechas circulares, visible en una versión
editable) y publicó **Version 3**, ID `01a0b8e2-45aa-7727-b8f5-52300867d7c5`.

Con la nueva clave literal en `X-API-Key`, el webhook aceptó `{"to":"","message":""}` y creó
el run `2bf83087-a39b-4865-b1bb-1c4373eb6e41`. El trigger terminó `succeeded` y Send SMS falló
con **`to is required`**, como se esperaba para evitar un envío real. Esto confirma acceso y
arranque del workflow, no entrega ni aceptación por Telnyx con un destinatario válido.

Se actualizaron los IDs locales en `sms-workflow.json`. Las claves siguen solo en variables
de proceso; la interfaz requiere reiniciar con ambas credenciales para volver a utilizarse.
No fue necesario desactivar Enhanced Security. La causa interna de los 401 anteriores no
se demostró: regenerar desde la UI y publicar sí resolvió el acceso; solo republicar no.

**Pendiente ahora:** prueba manual desde el botón con destinatario/texto autorizados y
confirmación de recepción en el móvil. No marcar un run completado como entrega confirmada.

### Envío SMS autorizado y bloqueo del proveedor

El usuario autorizó pruebas a su móvil. El run `04f49ede-8749-4017-8097-825957f46efd` falló con
Telnyx 40013 (origen inválido). La configuración inicial usaba el ID interno del teléfono como
valor estático; se creó Version 4 (`01a0b8e9-d59e-7de5-8a01-e8b692f285e2`) con el número E.164.
El siguiente intento autorizado, run `8b3443b5-9471-4c6c-b487-5956ebddf810`, falló con Telnyx
40305: remitente no asociado al perfil de mensajería. No se confirmó ninguna entrega.

El número es gestionado por HappyRobot y la API pública no expone la asociación necesaria.
El usuario compró por su cuenta un número Twilio normal, pero el selector gestionado de Send SMS
solo muestra toll-free y dice que no hay ninguno disponible. Se dejó SMS pendiente del mentor,
sin comprar números ni repetir intentos idénticos. La interfaz ahora traduce 40013/40305 sin
exponer las respuestas crudas del proveedor; 15 pruebas SMS pasan.

### Alternativa Telegram solicitada por el usuario

Petición: «hagamos mientras el telegram pues, cuando llegue un mentor lo intento arreglar».
El usuario confirmó que ya creó el bot en BotFather; no se solicitó publicar el token en el chat.

1. Se creó `telegram_demo.py`, reutilizando las protecciones HTTP y deduplicación de SMS, con
   `static/telegram.html` y `static/telegram.js`, estilos compartidos y puerto local 8092.
2. Se creó y publicó `FlareAI Telegram Demo`, ID `01a0b90b-bc5e-7b7c-b080-71f35f139200`,
   slug `qinusphyej9n`: request autenticado → HTTP POST a Telegram sendMessage. IDs no secretos
   en `telegram-workflow.json`. El token se configura como variable oculta de producción solo
   después de consentimiento en la página local y verificación de getMe/getWebhookInfo.
3. La vinculación usa un enlace start con nonce y caducidad de 10 minutos. Acepta un chat privado
   y comprueba usuario/fecha; ignora grupos y otros mensajes. No se envían mensajes al vincular.
   Un bot que ya tenga webhook se rechaza, sin borrarlo. El destinatario no es editable al enviar.
4. Cada mensaje lleva prefijo obligatorio de SIMULACRO y se envía solo después del botón con
   confirmación. JSON completo serializado en un campo de texto para evitar el formato Go de
   objetos anidados entre nodos de HappyRobot. No hay rutas de evacuación inventadas ni ES-Alert.
5. El primer probe del nuevo webhook volvió a devolver 401 con la clave UUID configurada por API.
   Se pidió regenerarla desde la UI en una versión editable y publicar. Se añadió un botón local
   que descubre la versión Live y su clave sin mostrarlas y comprueba con chat_id=0. El envío
   permanece bloqueado hasta comprobar la autenticación, conectar el bot y vincular el chat.
6. Pasan 27 pruebas sin red (15 SMS + 12 Telegram), compilación Python y ESLint; la página local
   responde HTTP 200. Se arrancó el servidor y se indicó al usuario cómo conectar su bot allí.

**Pendiente Telegram:** regeneración/publicación de la clave del webhook desde la UI, conexión
privada del token, vinculación del chat y recepción real. No afirmar entrega por un run completado.
El token no se guarda en archivos ni localStorage; sí queda en HappyRobot como variable oculta.
Las credenciales de cuenta viven en el proceso. Reiniciar pierde bot/chat/deduplicación locales.

### Telegram conectado y prueba directa autorizada

El usuario pidió encargarse de toda la configuración y proporcionó el token del bot. Se introdujo
por entorno de un proceso y mediante el endpoint local protegido, sin escribirlo en archivos.
El bot `HSFlareAlertBot` quedó verificado y el chat privado del usuario se vinculó tras abrir el
enlace start. El token quedó en la variable oculta de HappyRobot según lo autorizado.

Para resolver autónomamente la autenticación se comprobó que la clave válida de SMS no sirve
para el nuevo webhook; tampoco funcionó sustituir la clave creada por API por una UUIDv7 y
publicar Version 2 (`01a0b91c-c97c-7eaa-ad88-a2f27b5aaeeb`). No atribuir el fallo a la versión
UUID: ese ensayo no lo resolvió. Enhanced Security permanece activado. La API de test de nodo
exige versión no publicada y no admite inputs por ejecución, por lo que no se reutilizó como
ruta de envío ni se dejaron chats reales en muestras de test.

Se hizo **un único envío directo de diagnóstico con Telegram Bot API**, anunciado expresamente
como distinto del workflow de HappyRobot. El mensaje era un simulacro y aclaraba que la integración
seguía en verificación. Telegram respondió `ok: true`, `message_id: 2`, para el chat vinculado.
Esto confirma aceptación del mensaje por Telegram, **no lectura ni ejecución por HappyRobot**.
Pendiente confirmación del usuario y resolver la autenticación del workflow. La web mantiene
bloqueado su botón de envío por HappyRobot. No se desactivó seguridad ni se cambió a envío directo
silenciosamente. El token pasó por el chat: rotarlo al terminar la prueba, sin copiarlo al vault.

## Atlas nacional de infraestructura de emergencias

Petición: construir de forma autónoma una base local, real y georreferenciada de hospitales,
urgencias, bomberos, seguridad, Protección Civil, coordinación y helipuertos para toda España,
con SQLite, GeoJSON, extracción reproducible y futura integración agéntica.

1. Se revisó FlareAI y se mantuvo el nuevo proceso independiente del servidor, de ecCodes y del
   código archivado. No se hicieron llamadas, despachos ni cambios en HappyRobot para esta tarea.
2. Se investigaron datos.gob.es, Sanidad/CNH (basado en REGCESS/SIAE), portales autonómicos y
   Overpass. Se incorporaron 12 fuentes/datasets: OSM, hospitales y urgencias de Sanidad,
   bomberos de Madrid y ocho capas DERA de la Junta de Andalucía. No se afirma haber extraído
   todo REGCESS ni conectado todos los portales autonómicos investigados.
3. Se implementó `build_emergency_db.py`: adaptadores HTTP con reintentos, evidencia original
   comprimida por SHA-256, modos caché/offline/refresh, validación de respuestas completas,
   limpieza, cruce provincial, deduplicación conservadora, SQLite/RTree y GeoJSON.
4. Se resolvieron discrepancias reales: el CSV de Madrid anuncia UTF-8 pero contiene bytes
   Windows-1252; el RDF hospitalario incluye una descripción del esquema TAC que no es un
   centro; los límites provinciales llaman `Coruna` a A Coruña y requieren alias explícito para
   no perder Galicia. En urgencias se separó el nombre del centro cuando estaba publicado al
   inicio de la dirección, conservando el tipo de servicio y la evidencia literal.
5. Las búsquedas amplias por nombre agotaron el tiempo de algunos servidores Overpass. Se
   añadió un plan de consultas en 14 teselas para nuevas descargas y reutilización de respuestas
   nacionales completas existentes. La entrega final se reconstruyó sin red con las respuestas
   nacionales finalmente recuperadas, sin aceptar respuestas truncadas.
6. Se conservaron todos los registros oficiales sin coordenadas en SQLite. No se geocodificó al
   centro del municipio ni se añadieron teléfonos o disponibilidad supuestos. Las fusiones
   guardan método, puntuación, distancia y registros originales; los valores oficiales tienen
   prioridad descriptiva, pero no sustituyen coordenadas cuando no las publican.
7. Pasaron 25 pruebas unitarias sin red, Ruff, mypy y compilación. `--verify` comprobó hashes,
   integridad/claves externas, equivalencia DB/GeoJSON/RTree y consultas comparadas con barrido
   completo en Madrid, Canarias, Baleares, Ceuta y Melilla. La suite general no pudo importar
   los módulos meteorológicos por ausencia de ecCodes; no es una dependencia del atlas.
8. Se documentaron esquema, consultas SQL/Python, condiciones de reutilización, actualización,
   publicación y migración futura a PostGIS en `README.md` y `docs/IMPLEMENTACION.md`, sección 10.

**Resultado local:** `emergencias_espana.db` (95,2 MB), `emergencias_espana.geojson` (26,6 MB) y
`emergencias_espana.manifest.json`. Se conservaron 25.730 registros originales, agrupados en
25.136 entidades: 22.575 con coordenadas y 2.561 sin localizar. Hay puntos en las 52 provincias
y ciudades autónomas y las 19 comunidades/ciudades autónomas. Las categorías pueden solaparse:
1.277 hospitales, 1.270 entidades con urgencias, 1.086 parques de bomberos, 13 bases forestales,
4.974 entidades policiales, 286 de Protección Civil, 47 de coordinación, 337 de ambulancias,
523 de salvamento, 1.828 helipuertos y 107 humanitarias, además de 11.903 centros sanitarios.

```bash
python -m unittest test_emergency_db -v
python build_emergency_db.py --offline
python build_emergency_db.py --verify
python build_emergency_db.py --near 40.4168 -3.7038 15 --category hospital --limit 10
```

**Pendiente / limitaciones:** el objetivo de inventario exhaustivo y plenamente georreferenciado
no está certificado. Faltan posiciones fiables para 499 hospitales y 2.062 dispositivos de
urgencias de Sanidad. Entre los puntos faltan 14.628 municipios, 15.334 teléfonos normalizables,
3.057 nombres y 15 provincias. Las instantáneas OSM van del 06/05/2026 al 19/09/2026: una descarga
reciente no prueba vigencia de la instalación. Ampliar fuentes oficiales autonómicas, resolver
geocodificación con revisión y contrastar disponibilidad/competencias antes de una integración
operativa. `operational_status=not_verified` y `dispatch_authorized=false` son intencionados.

La publicación es atómica por archivo, no para el par DB/GeoJSON: verificar el manifiesto antes
de consumir una entrega o construir en un directorio de versión nuevo. Mantener un solo escritor
por caché/salida. En Windows se cierran explícitamente conexiones SQLite antes del reemplazo;
al detener un proceso desde una shell hay que comprobar que no queda su hijo Python antes de
retirar un bloqueo. Las respuestas originales permanecen en `data/emergency_sources/`.

## FlareAI actual: contexto territorial del atlas

Petición: integrar `Codigo_Atlas_Espana.zip` y `Espana_Datos_y_Mapas.zip` para señalar población,
instalaciones y elementos a tener en cuenta alrededor de cada foco, sin recargar el mapa y sin
base de datos por ahora. El proyecto activo está en la raíz; la implementación de las secciones
anteriores está apartada en `versión-anterior/` y no se modifica.

1. Revisados documentación, diccionario, scripts de descarga/cruce/clasificación/mapas/validación
   y resultados del paquete completo. Comprobados sus SHA-256. Conservado íntegro en
   `data/Espana_Datos_y_Mapas/`: 511.226 celdas y 44.787 elementos OSM.
2. `context.py` carga los CSV una sola vez con las dependencias existentes. Se descartó ejecutar
   el pipeline GIS porque el ZIP ya incluye resultados y eso evitaría descargar 2–3 GB e instalar
   nuevas dependencias de producción. `GET /api/context?id=<id>` permite cambiar posteriormente
   la capa de almacenamiento sin rehacer el panel.
3. Proximidad a 5 km de la huella térmica (no solo su centro); población sumada por celda una vez,
   cobertura del suelo ponderada por superficie clasificada. Población 2021, suelo 2019, OSM 2026.
4. Aviso orientativo a favor del viento: procedencia +180°, sector ±30°, mínimo 3 km/h. No se
   activa con meteo ausente/desactualizada; offline se identifica como histórico. No se estima
   población afectada, propagación ni riesgo oficial. Los campos de sexo/origen/movilidad no
   intervienen en la clasificación. Se mantienen fuentes y licencias en `/atlas/sources`.
5. Panel «A tener en cuenta» y botón «Ver entorno en el mapa», máximo 6 celdas habitadas y
   6 instalaciones. El resto de los datos se resume sin dibujar miles de puntos. Las coordenadas
   censales son centros de celdas, no nombres/ubicaciones exactas de núcleos de población.
6. Verificados 24 tests Python y 23 JavaScript, Ruff, mypy y ESLint. Comprobación real con Chromium
   en escritorio y móvil: seleccionar, acercar, ocultar puntos, abrir popup y cambiar de zona.
   Sin errores JavaScript. Node/Chromium de pruebas en `.local/`, paquetes Python en `.venv/`.

**Resultado:** servidor en `http://127.0.0.1:8090` en modo online, con contexto estático local.
En Igea hay contexto cercano sin aviso direccional por viento débil; Tarragona sí muestra
atención orientativa por elementos a favor del viento disponible al comprobarlo.

```bash
.venv/bin/python app.py --host 127.0.0.1 --port 8090
.venv/bin/python -m unittest -v
.local/node-v22.19.0-darwin-arm64/bin/node --test test_*.mjs
```

**Pendiente:** migrar el almacenamiento cuando se solicite; actualizar las instantáneas de fuentes
cuando corresponda. Revisar condiciones GISCO antes de reutilización comercial. Esta funcionalidad
no incorpora las capas oficiales EFFIS ni un modelo validado de propagación.

## Simplificación visual: potencial automático

El usuario pidió quitar el panel, los puntos y el botón de entorno: quiere una vista mínima,
tipo centro de control, con zonas de potencial automáticas al pulsar un incendio. Los detalles
quedan disponibles para una IA futura, no expuestos como nuevas tablas en la interfaz.

- Se sustituyó el panel por áreas difuminadas ámbar/coral y una leyenda pequeña. Seleccionar
  un foco encuadra automáticamente la huella +5 km. Los refrescos no mueven la cámara; en móvil
  la selección deja visible el mapa en vez de abrir una ficha encima.
- API v2 incorpora `potential`: modelo `attention-potential-v1`, muestras completas, evidencia,
  contribuciones, datos ausentes, score y zonas GeoJSON con referencias a muestras. Los pesos
  están centralizados en `POTENTIAL_MODEL`; proximidad, población, vegetación, instalaciones
  y viento vigente contribuyen, no solo la dirección del viento. No se usa la lista truncada
  de 12 puntos para dibujar ni evaluar.
- Decisión: llamarlo **potencial de atención**, no porcentaje de riesgo ni orden de intervención.
  Los pesos son heurísticos no calibrados. El área difuminada es soporte visual, no perímetro
  de peligro. No se aumenta el score por viento histórico/desactualizado ni se suman elementos
  OSM de una celda para inflar la contribución industrial.
- Verificación: 29 tests Python y 24 JavaScript, Ruff, mypy, ESLint; navegador real en escritorio
  y móvil, selección automática, áreas no interactivas y ausencia de controles anteriores.

## Mapa de calor real y huella estable al hacer zoom

Dos peticiones: que el entorno sea un mapa de calor de verdad (las áreas anteriores parecían
círculos difuminados) con rótulo al pasar el ratón, y que la huella del incendio deje de
«moverse» al hacer zoom.

- `static/heat.js`: acumulación de degradados radiales en opacidad y traducción de la densidad
  con una paleta de 256 pasos (crema → ámbar → coral). Canvas al 60 % de resolución, desenfoque
  CSS, composición `multiply` bajo el fuego, un repintado por fotograma con `requestAnimationFrame`.
  Se descartó dibujar polígonos por banda: producía discos superpuestos, no un degradado continuo.
- Rótulo al pasar el ratón con `nearestSample` + `hoverSummary`: dice qué hay (instalaciones por
  nombre, residentes, vegetación, viento actual y distancia). Se oculta al arrastrar, iniciar zoom,
  salir del mapa o vaciar la selección; nunca inventa viento no vigente.
- **Causa del efecto raro al hacer zoom**: `flames.js` ampliaba las componentes pequeñas con
  `factor = max(1, 7 / extent)`, que variaba de forma continua con la escala, así que la silueta
  se encogía y deformaba respecto al mapa. Además la ondulación se indexaba por número de vértice
  y `detailedOutline` remuestrea según el perímetro en píxeles, de modo que el patrón cambiaba al
  cambiar de zoom. Solución: dibujar la geometría real (el halo, con mínimo de 9 px, mantiene la
  legibilidad), indexar la ondulación por posición relativa del contorno y hacer su amplitud
  proporcional a la extensión dibujada. Prueba de regresión: los límites geográficos del contorno
  dibujado se conservan entre zoom 11 y 14.
- Detalle del idioma detectado en pruebas: en español «1200» se escribe sin separador de millares
  y «24.509» sí lo lleva; el rótulo usa `toLocaleString("es-ES")`, no un formato inventado.
- Verificación: 29 tests Python y 34 JavaScript (8 nuevos de `test_heat.mjs`), Ruff, mypy, ESLint y
  navegador real: 117.420 píxeles de calor en Tarragona, escalado correcto al alejar y acercar,
  limpieza al cerrar y funcionamiento en móvil, sin errores de JavaScript.

## Simulador 112 independiente con HappyRobot

Petición: crear en una carpeta superior independiente un asistente de voz que simule una llamada
al 112 y muestre en la misma página un resumen mínimo por puntos de lo que dice el llamante.

1. Se revisaron OpenAPI, vault e implementación de voz anterior. Se eligió
   `inbound-voice-agent` porque es una web call y no exige SIP ni un teléfono real.
2. Se creó `happyrobot-112/`, sin dependencias ni imports de FlareAI: backend Python stdlib,
   frontend HTML/CSS/JS y pruebas del extractor.
3. Se creó y publicó por API `Simulador 112 · Asistente de voz`
   (`01a0b8bc-b078-7eb0-86fc-b91a7abb6ca8`), voz Ana HR, español de España.
4. La primera versión advertía por voz que era una demo y terminaba cada turno con una ficha de
   cinco campos. La web obtiene la sesión desde runs/sessions/messages y no mantiene otra
   transcripción local.
5. Seguridad: API key solo en `happyrobot-112/.env`, ignorado por Git; el navegador recibe un
   token LiveKit temporal. La key compartida en chat queda pendiente de rotación.
6. Se comprobó creación/publicación, token, respuesta `waiting`, extracción, sintaxis
   Python/JavaScript e interfaz de escritorio/móvil.

### Optimización tras la primera llamada

La llamada real mostró dos fallos: el prompt ordenaba derivar al «112 real» y el agente pronunciaba
«Ficha… pendiente…». Además, al interrumpirlo, los mensajes quedaban cortados y la web solo
recuperaba ubicación.

- El agente ahora mantiene el rol de operador 112 y nunca deriva al mismo número. Extrae varios
  datos de una explicación, pregunta una sola cosa crítica y responde en frases breves.
- Schema de configuración consultado en vivo: se eligió `gpt-5.6-sol-low`, modelo frontier con
  esfuerzo bajo para reducir latencia; `transcriber_tier=advanced`, Ana HR v3 y español.
- Se eliminó toda ficha de la respuesta hablada. Nueva tool silenciosa
  `actualizar_ficha(ubicacion, emergencia, personas, riesgos, contacto)` con hijo Python sin
  efectos externos. La web lee `tool_calls`, fusiona correcciones y conserva el parser anterior
  solo como respaldo.
- Polling de 900 ms y caché del `session_id`, para una sola consulta de mensajes por refresco.
- La web pasó a ser una app de llamadas móvil: marcador oscuro, teclado `1-1-2`, pantalla de
  «Emergencias», estado, cronómetro y colgar. **Datos recopilados** abre una hoja inferior; la
  ficha permanece oculta el resto del tiempo.
- Replay de la llamada problemática: ubicación `calle Rosetas, 18`, emergencia `Incendio / fuego`
  y riesgos `personas atrapadas; peligro vital indicado`. El caso exacto `Ficha:` vacío descubrió
  una regresión y quedó cubierto.
- Verificación: workflow live con tool completa, llamada WebRTC en estado `En llamada`, apertura
  «Emergencias 112…», flujo visual completo del marcador y seis pruebas unitarias verdes.

**Resultado:** servidor en `http://127.0.0.1:8112`, workflow live y conversación separada de la
ficha estructurada. Véase [[Simulador 112 con ficha en directo]].

### Repaso de la interfaz como app de teléfono

Petición: «que sea igual a una app de teléfono, el símbolo de llamar no me gusta nada» y revisar
que se vea bien en móvil.

- Los iconos eran glifos tipográficos (`☎` girado, `⌫`, `⌨`, `◉`, `♩`), que dependen de la fuente
  del sistema y se ven distintos en cada plataforma. Se sustituyeron por iconos Material en SVG
  declarados una vez como `<symbol>` y reutilizados con `<use>`: llamar, colgar, borrar,
  micrófono, micrófono silenciado, altavoz, teclado, chevron y cerrar.
- Dos fallos reales encontrados al revisar, no solo cuestión de gusto:
  - El CSS usaba `gap-block`, que no existe (la propiedad es `row-gap`), así que la compactación
    vertical del teclado en pantallas bajas nunca se aplicaba.
  - `.caller p` ganaba en especificidad a `.call-state`, de modo que «En llamada 00:47» heredaba
    los 27 px del nombre del contacto. El nombre pasó a la clase `.caller-name`.
- También se añadió `text-size-adjust: 100%`: el autoajuste tipográfico de Chrome en móvil
  inflaba textos pequeños al pintarlos, aunque el layout calculara 13 px.
- Medidas sustituidas por `clamp(mín, min(vw, dvh), máx)` para que teclado y botones encojan con
  la altura real del móvil, más `env(safe-area-inset-*)` y `viewport-fit=cover` para el notch.
- El botón de silenciar dejó de ser decorativo: llama a `setMicrophoneEnabled` y cambia icono y
  etiqueta. Altavoz y teclado quedan visiblemente `disabled` en vez de fingir que funcionan.
- Comprobado en navegador real a 320×568, 375×667, 393×852 y 412×915: sin scroll vertical ni
  horizontal en ninguno, teclas de 49 a 68 px, botón de llamar de 54 a 68 px y aviso inferior
  siempre visible. En escritorio se centra un marco de 400 px.

## FlareAI: SQL local, instalaciones, cámaras y carreteras

Peticiones: conservar el heatmap y añadir fichas de instalaciones; incorporar el ZIP Faro de
webcams; añadir carreteras fijas; mantener los fuegos visibles al alejarse; gris para detecciones
sin confirmar; preparar los datos en SQL local para una futura migración a HappyRobot.

- Se contrastaron [[Twin - base de datos]], [[Base de datos local embebida]] y el OpenAPI público
  oficial de HappyRobot. La documentación web sigue protegida por código. No se consultaron
  credenciales ni se conectó a Twin. Tipos admitidos comprobados: text, float8, int8, timestamp,
  uuid, boolean y jsonb; SQL por `POST /twin/sql`.
- PostgreSQL 17.11 independiente en `.local/pg`, solo socket UNIX, sin escucha TCP ni alteración
  del clúster SOS. Psycopg 3.3.2 en `.venv`. `schema.sql` registra la migración inicial y usa tablas
  `flare_`, PK/FK e índices. Se importaron 511.226 celdas (47.400.134 residentes censados), 44.787
  instalaciones y 2.926 cámaras; importación transaccional e idempotente. Todos los campos
  originales del atlas se conservan, con coordenadas/población también como columnas numéricas.
- El servidor usa SQL para datos activos y metadatos: instantáneas FIRMS/GFS, focos, observaciones,
  configuración, catálogos, cartografía, confirmaciones y medios. Imágenes/teselas/GRIB siguen en
  disco. La consulta geográfica prefiltra con índices y después mide desde la huella con Shapely.
  Se comparó el resultado contra el atlas completo. Se fijó el orden por ID para que SQL y CSV
  produjeran evidencia/geometrías deterministas, sin cambiar puntuaciones.
- Instalaciones: iconos por actividad, agrupación según zoom y ficha con nombre, actividad,
  distancia, viento válido, prioridad orientativa, fecha y enlace OSM. La posición puede ser
  representativa; no se presenta como riesgo oficial. Los grupos coincidentes permiten elegir.
- Cámaras: DGT 1.948, Madrid 357, Euskadi 295, Hispacams 164, SCT 130, MeteoGalicia 31 y Canarias 1.
  Se consulta la imagen solo al abrir, con TTL, allowlists, validación de redirecciones, límites
  de tamaño/tiempo/concurrencia y validación Pillow. Se muestran recuperación y Last-Modified,
  sin confundirlos con hora de captura. Reproductores públicos tras clic; enlace si no integrables.
- Carreteras: WMS SCNE/IGN `TN.RoadTransportNetwork.RoadLink`, EPSG:3857, teselas visibles
  256×256 con caché de 30 días. Capa siempre activa, estilo suave y atribución CC BY 4.0.
  No se descargó la red vectorial completa; no es una capa de cortes ni de rutas seguras.
- Las detecciones sin confirmar se dibujan grises, incluidas chispas/resplandor/lista. La
  confianza alta de FIRMS no confirma incendio forestal y la noticia de Igea es histórica.
  `flare_confirmations` exige fuente, fecha, caducidad y estado; la retirada o caducidad prevalece.
  No hay proveedor automático de confirmaciones conectado: las 44 detecciones disponibles en la
  comprobación permanecieron grises. Un símbolo de llama de hasta 32 px mejora la vista nacional
  sin modificar la huella geográfica; la regresión de zoom 11→14 sigue pasando.
- Pruebas: 39 Python, incluyendo cinco con PostgreSQL; 39 JavaScript; Ruff/mypy/ESLint. Navegador
  real de escritorio/móvil: carreteras, instalación Moeve en Tarragona, captura DGT A-62 km 25,3,
  cierre del visor y layout sin desbordamiento. Se corrigió un popup que crecía bajo la cabecera:
  ahora recalcula posición al cargar el medio y oculta temporalmente el título superpuesto.
- Maestro conservaba una sesión Chromium desconectada; se verificó con Playwright 1.55 ya
  instalado en `.venv`, con navegador bajo `.local/playwright-browsers`, sin instalar globalmente.
- Exportación completa ejecutada en `.local/export-20260919`: esquema y JSONL por tabla mediante
  cursor, en transacción consistente. No se borró ni sobrescribió la base. El servidor sigue
  online en :8090; logs en `.local/flareai-server.log`.

**Límites y próximos pasos:** el catálogo de webcams es una instantánea importada, no todas las
webcams de España ni una sincronización de catálogos. El SQL es compatible en tipos, pero la
migración real a Twin requiere permisos/provisionamiento, importador por lotes y prueba de límites;
no se ha ejecutado remotamente. Hay que copiar los medios aparte. Para producción faltan servidor
HTTP robusto, pooling, backups operativos, rate limiting y pruebas de carga. La implementación
independiente `happyrobot-112/` no se modificó ni se mezcló con esta base.

Comandos actuales:

```bash
.venv/bin/python database.py start
.venv/bin/python database.py import
.venv/bin/python database.py stats
.venv/bin/python database.py export --directory .local/export-NUEVO
FLAREAI_TEST_DATABASE=1 .venv/bin/python -m unittest -v
```

## Afinado visual, cámaras verificadas y carreteras estables

El usuario pidió ocultar cámaras en el panorama, recuperar la animación de fuego en vez del
icono estático al alejar, mostrar solo cámaras utilizables dentro del mapa e impedir que las
carreteras parpadearan. Reiteró: confirmado rojo, no confirmado gris.

- Se revisaron los commits y el ZIP original. La animación no se había quitado: al alejarse
  quedaba pequeña bajo un símbolo estático. Ahora `fireDisplayScale` aplica a toda la huella un
  factor común para conservar un radio visual mínimo de 16 px, incluidos huecos y chispas. Por
  encima de ese tamaño el factor es 1. No cambia la geometría medida. Las pruebas comprueban
  animación de lejos, tamaño mínimo y estabilidad geográfica en detalle.
- Cámaras invisibles por debajo de zoom 10, pero conservadas en SQL. Al alejarse se cierra el
  visor y se retiran sus medios. El catálogo visible se refresca cada minuto.
- Migración 2: `flare_camera_checks`, con FK de cámara, método, fecha, estado y caducidad.
  El endpoint visible requiere evidencia vigente: imagen válida o reproducción de vídeo probada.
  Un HTTP 200 y un elemento `<video>` no bastan. Fallos y medios solo externos quedan ocultos.
- Auditoría de **los 2.926 registros**. Resultado al cerrar: **2.240 capturas disponibles**
  (DGT 1.812, Madrid 312, SCT 85 y MeteoGalicia 31), 602 no disponibles y 84 externos/restringidos.
  No se borró ninguna cámara. Los fallos se reevalúan y los recuentos pueden cambiar.
- Euskadi redirige a `www.trafikoa.euskadi.eus`: se añadió únicamente ese host/ruta oficial.
  Tras admitir la redirección, las 295 URLs probadas siguieron fallando; no se inventó cobertura.
- Se detectaron seis imágenes de error distintas de DGT, SCT/Barcelona y Madrid, que devolvían
  HTTP 200. Se comprobaron visualmente y se excluyen por SHA-256 tanto en descarga como en caché
  y catálogo. La lectura remota usa `read1` para poder comprobar el límite de tiempo entre
  fragmentos, incluso si una respuesta llega lentamente.
- Se resolvieron 160 reproductores públicos y se probaron en Chromium dentro de un iframe local.
  81 restringían explícitamente los dominios; 79 no ofrecieron fotogramas con tiempo avanzando,
  incluso tras un segundo intento con el control Play. No se falseó Referer, no se extrajeron
  tokens/manifiestos ni se presentaron carteles o miniaturas antiguas como cámaras actuales.
  También se busca una captura pública en `img#player`, sin confundirla con imágenes de anuncios
  o cámaras relacionadas. El enlace canario HTTP no se considera integrable en HTTPS.
- Comprobaciones limitadas: cuatro workers, dos por fuente; el servidor reevalúa hasta 200 medios
  y 20 vídeos por ciclo de diez minutos, priorizando antigüedad. Locks SQL evitan duplicar trabajos.
  Una prueba de vídeo caducada no se renueva solo por volver a consultar su HTML.
- **Causa del parpadeo de carreteras:** el zoom interpolado invoca `map.setView` por fotograma;
  Leaflet emite `viewprereset` y su GridLayer destruía todas las teselas. `StableRoadLayer` elimina
  solo ese handler, conserva reproyección y retención de padres/hijos. La prueba real retuvo
  12/12 nodos al variar el zoom fraccional y nunca bajó a cero teselas cargadas al cambiar de nivel.
- Verificación: 45 pruebas Python, 42 JavaScript, Ruff, mypy y ESLint. Navegador de escritorio y
  móvil, cámara DGT real integrada, cierre automático al alejar y ausencia de errores JS o
  desbordamiento. La clasificación rojo/gris se mantiene independiente del tamaño visual.

```bash
.venv/bin/python territorial.py verify-cameras
.venv/bin/python verify_camera_players.py
.venv/bin/python verify_camera_players.py --retry-failed
```

Los resultados prueban la respuesta/medio en el momento de comprobación, no disponibilidad
permanente ni que una imagen no esté congelada. Se mantienen las fechas del proveedor. Las notas
anteriores describen estados históricos; esta sección refleja las preferencias visuales actuales.

## Cierre de la integración webcall 112 → mapa

Petición: «acaba esto», retomando el handoff de la webcall sin código, demo limpia por arranque,
túnel móvil aislado y confirmación automática del mapa, sin commit del agente.

1. Se revisaron código, documentación y procesos. El repositorio estaba limpio al comenzar,
   a diferencia del handoff; no había mapa ni túnel activos. `happyrobot-112/.env` y herramientas
   locales presentes, sin mostrar credenciales ni tocar la implementación apartada.
2. Se arrancó `app.py` online en :8090 con marcador aislado en :8112 y se lanzó `demo.py publish`.
   El Quick Tunnel funcionó tras un reintento inicial; solo publica el puerto móvil. La sesión
   arrancó con cero llamadas y cero confirmaciones demo.
3. Se consultó el workflow por GET, sin alterarlo: publicado/live. Chromium comprobó el HTTPS
   público, SDK LiveKit cargado, sesión automática sin código, cookie segura y cero errores JS.
   `/api/data`, `/api/demo/setup`, `/schema.sql`, `/112/../demo.py` y `/112/.env` dieron 404 desde
   el túnel; el enlace del mapa apuntó al marcador público.
4. Se añadieron dos regresiones: reinicio de `Store` con PostgreSQL conservando la llamada
   anterior pero sin restaurarla ni aceptar su cookie, y exclusión de variables de proveedor/
   Cloudflare del subproceso. La prueba SQL usa rollback; no borra historial real.
5. Se entregó la URL al usuario y entró una **webcall real sobre Tarragona**. El backend extrajo
   `actualizar_ficha` desde HappyRobot y creó `Aviso · Tarragona` automáticamente. La API real
   mostró mensajes de usuario/asistente y tool; no se inyectaron mocks en el servidor activo.
   Chromium comprobó el aviso seleccionado sin pulsar el mapa, clase `confirmed`, etiqueta
   **Confirmado por llamada · demo** y cero errores JavaScript. Sin observaciones ni área NASA
   inventadas, ni cambio de registros FIRMS a confirmaciones oficiales.
6. Pasaron **57 pruebas Python raíz con SQL, 6 del marcador, 42 JavaScript**, Ruff, mypy y ESLint.
   Se actualizaron README, AGENTS, implementación y [[Simulador 112 con ficha en directo]] con
   arquitectura, límites, arranque sin código y resultado real; el índice refleja el estado actual.
7. La actualización online generó cambios de caché/evidencias. El agente no hizo commit ni push;
   durante la sesión se detectó un commit externo `e24abb8` que recogió cambios existentes.
   No se reescribió historial ni se revirtieron esas modificaciones.

**Resultado al cierre:** mapa y túnel quedan activos para continuar la demo. El flujo real de
HappyRobot al mapa está verificado; la URL es temporal y no debe usarse como despliegue permanente.
La API key permanece en backend. El escaneo del vault no encontró valores completos con patrones
de claves/Bearer; las referencias antiguas conservan únicamente placeholders o fragmentos.

**Pendiente / próximos pasos:** el usuario debe valorar si escucha bien el audio en su móvil.
No se midió latencia ni se hizo una matriz Safari/iOS. El caso de foco existente y las correcciones
están cubiertos por proveedor simulado; la llamada real creó un aviso nuevo. Para Vercel se necesita
mantener backend, SQL y proxy de API del mismo origen: publicar los estáticos no sustituye esos
procesos. Detener el túnel al terminar la demo para cerrar el acceso público sin contraseña.

## Director autónomo con rutas locales

Petición: mapa solo, paneles únicamente al usarse, instalaciones sin tapar el fuego, decisiones
reales en HappyRobot tras una llamada y recursos visibles sobre carreteras. Prioridad: demo clara
de hackathon; SMS descartado, cámaras/tráfico para después. No sustituir el LLM por reglas locales.

1. Se implementaron vista mínima, instalaciones desde zoom 13 con exclusión sobre huellas, feed de
   acciones, borde multicolor y vehículos con pausa/movimiento reducido. La ficha deja de abrirse
   por defecto; recargar no vuelve a presentar avisos viejos como nuevos.
2. `director.py` conserva estado, recursos ficticios y eventos en PostgreSQL (migración 4), valida
   IDs/revisión/disponibilidad y descarta planes obsoletos. Consulta sedes del atlas SQLite en solo
   lectura. ES-Alert es una vista previa, no un envío. No se modifica el workflow de voz.
3. A petición del usuario, se sustituyó el adaptador de routing externo por `local_routes.py`:
   adquisición OSM por zonas, caché SQL y A* aquí. Prueba Tarragona: 2,08 km, 85 vértices; adquisición
   y cálculo 3,11 s, caché sin red 0,005 s en este equipo. Sin ruta conectada, no hay despacho.
4. Workflow `FlareAI · Director autónomo demo`, ID `01a0b948-d14b-7883-bb58-2c9a014f27f4`.
   El webhook devolvía 401. El usuario regeneró desde UI/publicó y proporcionó la clave literal.
   El valor consultado por API en `configuration.api_key` era distinto del literal (formato de
   token transformado): **no sirve copiarlo automáticamente al header**. `sync` ahora conserva
   la clave literal almacenada en SQL. No se guardan secretos en este vault; rotar las credenciales
   compartidas por chat de manera coordinada, sin romper marcador/director.
5. El run `7ac10be7-af33-421e-974f-fbbd3462757b` llegó a ejecutar el agente, pero vio el contexto vacío.
   La salida real del hook contiene `data.context_json`; el prompt estaba leyendo `context_json`.
   Además el lector se detenía ante el wrapper `name`/`args` sin visitar `function.arguments`.
   Se reprodujeron ambos fallos con tests y se corrigieron. No era falta de capacidad del LLM.
6. Con autorización del usuario se publicó un fork corregido, versión
   `01a0b969-9df4-7789-87b5-d2cea2da3cff`, tras despublicar brevemente la anterior. El workflow de
   voz permaneció intacto. Tras fork, usar IDs persistentes de `available-vars` en el prompt,
   no asumir que coinciden con IDs de nodos.
7. Run real `87e54d53-bfb5-489e-8def-b3ab2d6f932b`: el LLM priorizó el aviso de vegetación con humo
   cerca de viviendas y eligió dos camiones del parque de Tarragona. El backend validó el plan y
   aplicó dos rutas locales de 2,08 km. Entrada de llamada fixture explícita; evidencia en el asset
   SQL `director-verification`. No se afirma que esta ejecución sea una llamada de audio real.
8. Pasaron 132 pruebas Python raíz con SQL, 6 del marcador y 45 JS; Ruff, ESLint y mypy del código
   cambiado. Mypy global conserva un fallo previo ajeno en `sms_demo.py:271`. Pruebas Chromium de
   escritorio/móvil, animación, pausa y recarga. El servidor 8090/8112 se reinició con autorización.
9. Segundo run real `1f123abe-6892-4598-a23f-c505012ba841`, integrado con Chromium durante la
   prueba `verify_director_ui.py --cloud`: plan del LLM, recurso visible y en movimiento, pausa,
   recarga sin repetir acciones y móvil verificados. La llamada seguía siendo fixture; no audio.
   El marcador local queda configurado. La URL temporal anterior del túnel dejó de resolver en
   la comprobación local, aunque su proceso seguía vivo; revisar/renovar antes de probar desde móvil.

Comandos de continuación:

```bash
.venv/bin/python director_workflow.py status
.venv/bin/python -m unittest test_director test_local_routes -v
PLAYWRIGHT_BROWSERS_PATH="$PWD/.local/playwright-browsers" .venv/bin/python verify_director_ui.py
.venv/bin/python verify_director.py --cloud
PLAYWRIGHT_BROWSERS_PATH="$PWD/.local/playwright-browsers" .venv/bin/python verify_director_ui.py --cloud
```

Los comandos `--cloud` consumen cuota y usan el LLM real; el verificador visual sin ese flag usa
planner fixture. La entrada de llamada en ambos verificadores es una muestra estructurada. Falta
la valoración humana de una llamada de voz completa tras el reinicio: recargar marcador y mapa.
Flota/tiempo de trayecto/alertas son demo. Carreteras sin tráfico, cortes, gálibos ni todos los giros.
Memoria por sesión, no reentrenamiento autónomo del modelo. No se hizo commit ni push.

## Seguimiento cinematográfico y evidencias visuales

Petición: evitar que el mapa se teletransporte al decidir, volver a mirar los diferentes avisos y
camiones, y mostrar las fuentes cercanas para que el trabajo del director sea visible. La ausencia
de datos no debe servir para desestimar una llamada de demo.

1. Se sustituyeron los encuadres instantáneos del director y del aviso entrante por tres fases:
   alejar, recorrer y acercar. Se interpolan centro proyectado y zoom, conservando las teselas IGN.
   El delta por fotograma queda limitado a 50 ms: una pausa del render no debe crear otro salto.
2. Tras actividad reciente, una ronda visual alterna avisos demo y vehículos en movimiento. No
   inventa decisiones del LLM ni dispara runs. Al tocar el mapa se suspende hasta pulsar
   «Reanudar seguimiento IA»; pausa, pestaña oculta y movimiento reducido se respetan.
3. Panel de revisión en `static/director.js`: busca observaciones FIRMS a ≤10 km de la ubicación
   comunicada, no solo centroides. Muestra distancia, fechas, máximo de brillo I4 del **grupo** y
   temperatura ambiente/viento NOAA GFS por separado. No se atribuye a la NASA la temperatura del aire.
4. Si hay FIRMS cerca, consulta el mosaico GIBS SWIR del entorno con opción de color natural y
   puntos de detección superpuestos. Sin coincidencia, no solicita automáticamente la imagen.
   El mosaico diario no confirma fuego y puede tener otra fecha que las detecciones.
5. Cámaras del catálogo ya verificado del mapa: captura cercana automática, o apertura explícita
   del vídeo del proveedor. Se conservan fechas, copia offline y atribución. No se integró Traffic
   Lab ni se añadió visión artificial. El texto distingue revisión visual de análisis de píxeles.
6. Consultas acotadas, caché corta, timeout y cancelación al cerrar/cambiar de zona. Las respuestas
   tardías no recuperan una ficha antigua; la caché satelital incluye coordenadas para no reutilizar
   imágenes tras corregir la ubicación de un mismo aviso. Los fallos no cambian llamadas,
   confirmaciones o planes.
7. La verificación reprodujo un fallo al limpiar el calor antes del primer encuadre: `heat.paint()`
   requiere un mapa inicializado para `getPixelOrigin()`. Se evita limpiar antes de tener zoom.
8. Pasan 54 pruebas JS y 34 Python de director/demo/rutas; ESLint, Ruff y mypy del verificador.
   Chromium comprueba vistas intermedias, alejamiento/acercamiento entre Igea y Tarragona,
   interrupción manual, reanudación, pausa, recarga sin repetir eventos y panel móvil sin cubrir
   el centro del mapa. GIBS usa imágenes reales **guardadas**; cámara y avisos son fixtures.
   No se ejecutó `--cloud`, no se gastó cuota HappyRobot ni se modificaron workflows remotos.

Verificación reproducible:

```bash
.local/node-v22.19.0-darwin-arm64/bin/node --test test_*.mjs
.venv/bin/python -m unittest test_director test_demo test_local_routes -v
PLAYWRIGHT_BROWSERS_PATH="$PWD/.local/playwright-browsers" .venv/bin/python verify_director_ui.py
```

La prueba deja `.local/director-evidence.png`. Pendiente: valoración humana del ritmo y aspecto
durante una llamada real, con la disponibilidad de cámaras y mosaicos online de ese momento.
No se ha probado ni se afirma detección de humo/fuego por IA sobre imágenes. Recargar el mapa
sin caché para recoger los módulos estáticos nuevos; el backend y el marcador no cambian.

Relacionado: [[00 Índice]] · [[Cómo arrancar todo]] · [[Motor de riesgo]] · [[Simulador 112 con ficha en directo]] · [[Twin - base de datos]]
